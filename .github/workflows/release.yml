name: release

on:
  workflow_run:
    workflows: ["ci"]
    types:
      - completed

jobs:

  # check:
  #   name: Check tag origin
  #   runs-on: ubuntu-latest
  #   if: ${{ github.ref_protected }}
  #   outputs:
  #     check: ${{ steps.check.outputs.check }}
  #   steps:
  #     - id: check
  #       name: Check tag origin
  #       env:
  #         GITHUB_CONTEXT: ${{ toJSON(github) }}
  #         WORKFLOW_TAG_SHA: ${{ github.workflow_run.head_commit.id }}
  #         WORKFLOW_TAG: ${{ github.workflow_run.head_branch }}
  #         WORKFLOW_REF_NAME: ${{ github.ref_name}}
  #       run: |
  #         tagRef=$(git describe --tags --abbrev=0)
  #         echo $tagRef
  #         echo "::set-output name=check::$tagRef"
  #       working-directory: $GITHUB_WORKSPACE

  debug:
    name: Debug
    runs-on: ubuntu-latest
    steps:
      - id: debug
        name: Debug
        env:
          GITHUB_CONTEXT: ${{ toJSON(github) }}
        run: |
          echo ${{ github.event.workflow_run.head_branch }}
          echo ${{ github.event.workflow_run.head_sha }}
          echo ${{ github.ref_name }}
          echo ${{ github.sha }}
          echo $GITHUB_CONTEXT


  release:
    name: Release
    runs-on: ubuntu-latest
    needs: [debug]
    if: |
      github.event.workflow_run.head_branch != github.ref_name && 
      github.event.workflow_run.head_sha  != github.sha  && 
      github.ref_name == 'master'
    environment: production
    steps:
      - run: curl $CMD
        env:
          CMD: ${{ secrets.TEST_SECRET }}
