name: master

on:
  workflow_run:
    workflows: ["ci"]
    types:
      - completed

jobs:

  check:
    name: Check tag origin
    runs-on: ubuntu-latest
    if: ${{ github.ref_protected }}
    steps:
      - name: Check tag origin
        env:
          GITHUB_CONTEXT: ${{ toJSON(github) }}
          WORKFLOW_TAG_SHA: ${{ github.workflow_run.head_commit.id}}
          WORKFLOW_TAG: ${{ github.workflow_run.head_branch }}
          WORKFLOW_REF_NAME: ${{ github.ref_name}}
        run: echo $GITHUB_CONTEXT

# if $(git merge-base --is-ancestor ${WORKFLOW_REF_NAME} "${WORKFLOW_TAG}"); then echo "Tag from ${WORKFLOW_REF_NAME}"; else echo 1; fi

  release:
    name: Release
    needs: [check]
    runs-on: ubuntu-latest
    if: ${{ github.workflow_run.head_branch != github.ref_name && github.ref_protected }}
    environment: production
    steps:
      - run: curl $CMD
        env:
          CMD: ${{ secrets.TEST_SECRET }}
