name: master

on:
  workflow_run:
    workflows: ["ci"]
    branches: [master]
    tags: ['*']
    types:
      - completed

jobs:

  dump:
    name: Dump output
    runs-on: ubuntu-latest
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJSON(github) }}
        run: echo "$GITHUB_CONTEXT"

  release:
    name: Release
    needs: [dump]
    runs-on: ubuntu-latest
    if: ${{ github.ref_protected && github.workflow_run.head_branch == 'master' }}
    environment: production
    steps:
      - run: curl $CMD
        env:
          CMD: ${{ secrets.TEST_SECRET }}
